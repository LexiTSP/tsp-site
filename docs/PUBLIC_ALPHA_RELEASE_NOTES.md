# TSP Public Alpha Release Notes

Date: 2026-05-13

## Current Verified Bundle

- 223 tests in the local release gate: 101 SDK, 24 TrustBadge, 31 Risk, 38 Evidence, 25 Oversight, 4 Control Plane.
- 55 static pages generated by the Next.js build, including public manifest endpoints and the privacy page.
- `bun run check:release` is the release gate: build, all package tests, backend typechecks, manifest assertion, local package smoke and production-start HTTP smoke.
- `@lexitsp/sdk` and `@lexitsp/trustbadge-react` are alpha/publish-ready from this workspace.
- Risk, Evidence and Oversight are commercial backend alphas for pilots.
- Control Plane is a hosted pilot alpha for operator-managed customers, service key rotation, health checks and metering export.
- There is no Studio SKU, route or pricing surface.

## Manual External Dependencies

Julian owns DNS, GitHub repository setup, npm scope/package publish and production hosting. Canonical public domain is `https://truststandardprotocol.com`; `truststandardprotocol.com` should redirect there and `tsp.lexico.no` remains a LexiCo-owned technical alias. After those are done, run the external install smoke from a clean machine.

Public GitHub shape:

- `lexitsp/tsp-site`
- `lexitsp/sdk`
- `lexitsp/trustbadge-react`

Only SDK and TrustBadge are public npm packages for alpha. Risk, Evidence, Oversight and Control Plane stay commercial/private.

## Pilot Proof

Run:

```sh
bun run pilot:golden
bun run pilot:hosted
```

These exercise the local pilot path and hosted pilot control-plane path: customer provisioning -> SDK TrustEnvelope -> Risk ingest/alarm -> Oversight review -> Evidence dossier -> metering.
