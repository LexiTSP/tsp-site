# TSP RFC 0000: Template

Status: template

## Summary

State the proposed protocol, fixture, or governance change in one paragraph.

## Problem

What concrete implementer, verifier, issuer, manifest, or governance problem does this solve?

## Proposed Change

Describe the change. Include schema diffs, check-name changes, fixture additions, or wording changes where relevant.

## Compatibility

Answer each point:

- Does any valid v3 envelope become invalid?
- Does any invalid envelope become valid?
- Does an existing verifier need code changes?
- Does an existing issuer need code changes?
- Are new fixtures required?

## Security and Privacy Impact

Explain effects on signatures, canonicalization, key lifecycle, timestamps, manifests, revocation, data minimization, and user tracking risk.

## Alternatives Considered

List at least one rejected alternative.

## Implementation Plan

Name the spec file, SDK file, fixture file, and docs page that would change.

## Decision

Accepted, rejected, or deferred, with rationale.
